# Docker environment variables
# Copy this file to .env and customize

# Password for code-server web interface
PASSWORD=tenstorrent

# Sudo password inside container
SUDO_PASSWORD=tenstorrent

# Optional: Path to tt-metal (if mounting from host)
# TT_METAL_HOME=/path/to/tt-metal

# Optional: Device configuration
# MESH_DEVICE=N150
# TT_METAL_ARCH_NAME=wormhole
