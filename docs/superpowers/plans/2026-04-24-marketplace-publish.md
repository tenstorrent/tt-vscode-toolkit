# Marketplace Publishing & Web Presence Update Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Surface the VS Code Marketplace as the primary install path, fix a wrong extension ID in the site build, and enable automated marketplace publishing on every tagged release.

**Architecture:** Six targeted edits across the build script, static install page, README, release workflow, and version files. The build script fix propagates to all generated lesson pages via a single `npm run build:web` run. No new abstractions introduced.

**Tech Stack:** Node.js (build-web.js), static HTML (site/install/index.html), Markdown (README.md, CHANGELOG.md), GitHub Actions YAML, npm/vsce

---

## File Map

| File | Change |
|------|--------|
| `scripts/build-web.js:402` | Fix wrong extension ID in `mktUrl` |
| `site/install/index.html:605-628` | Add marketplace hero button |
| `site/install/index.html:914-929` | Add Marketplace install tab |
| `site/install/index.html` (after line 929) | Add Marketplace tab panel |
| `site/lessons/**/*.html` | Regenerated by `npm run build:web` |
| `README.md:5-6` | Add marketplace badge |
| `README.md:98-128` | Restructure install options |
| `.github/workflows/release.yml:55-73` | Update release body text |
| `.github/workflows/release.yml:99` | Remove `if: false` |
| `package.json` | Bump version 0.0.406 → 0.0.407 |
| `CHANGELOG.md` | Add 0.0.407 entry |

---

## Task 1: Fix wrong extension ID in build script

**Files:**
- Modify: `scripts/build-web.js:402`

- [ ] **Step 1: Make the fix**

In `scripts/build-web.js`, find line 402 and change:
```js
    const mktUrl   = 'https://marketplace.visualstudio.com/items?itemName=Tenstorrent.tenstorrent-toolkit';
```
to:
```js
    const mktUrl   = 'https://marketplace.visualstudio.com/items?itemName=Tenstorrent.tt-vscode-toolkit';
```

- [ ] **Step 2: Regenerate the site**

```bash
npm run build:web
```

Expected: exits 0. Check that a few lesson HTML files now contain the correct URL:
```bash
grep -r "tenstorrent-toolkit" site/lessons/ | wc -l
# Expected: 0
grep -r "tt-vscode-toolkit" site/lessons/ | head -3
# Expected: multiple matches
```

- [ ] **Step 3: Commit**

```bash
git add scripts/build-web.js site/
git commit -m "fix: correct marketplace extension ID in site command badges"
```

---

## Task 2: Add marketplace hero button to install page

**Files:**
- Modify: `site/install/index.html` (hero CTA section, ~line 602)

- [ ] **Step 1: Add marketplace button styles**

In `site/install/index.html`, find the `<style>` block near the top and add after the `.install-dl-btn:hover` rule (after line ~113):

```css
.install-mkt-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  background: var(--tt-primary);
  color: #0a1420;
  font-weight: 600;
  font-size: 0.95rem;
  border-radius: 6px;
  text-decoration: none;
  transition: background 0.15s, transform 0.1s;
  white-space: nowrap;
}
.install-mkt-btn:hover {
  background: var(--tt-primary-light);
  transform: translateY(-1px);
}
.install-mkt-btn svg { flex-shrink: 0; }

.install-dl-btn-secondary {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  background: transparent;
  color: var(--tt-primary);
  font-weight: 500;
  font-size: 0.95rem;
  border-radius: 6px;
  border: 1px solid var(--tt-primary);
  text-decoration: none;
  transition: background 0.15s, color 0.15s;
  white-space: nowrap;
}
.install-dl-btn-secondary:hover {
  background: rgba(79, 209, 197, 0.08);
}
.install-dl-btn-secondary svg { flex-shrink: 0; }
```

- [ ] **Step 2: Replace the hero CTA block**

Find this block (starting ~line 603):
```html
      <div class="install-cta">
        <a id="download-btn"
           class="install-dl-btn"
           href="https://github.com/tenstorrent/tt-vscode-toolkit/releases/latest">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M8 1v9M4 7l4 4 4-4M2 13h12" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          Download latest .vsix
        </a>
        <a href="/lessons/" class="install-browse-btn">
          Browse Lessons — no hardware needed →
        </a>
        <span id="dl-version" class="install-version-badge"></span>
      </div>

      <p class="install-cli-note">
        Or via terminal:
        <code>gh release download --repo tenstorrent/tt-vscode-toolkit --pattern '*.vsix' &amp;&amp; code --install-extension tt-vscode-toolkit-*.vsix</code>
      </p>
```

Replace it with:
```html
      <div class="install-cta">
        <a class="install-mkt-btn"
           href="https://marketplace.visualstudio.com/items?itemName=Tenstorrent.tt-vscode-toolkit"
           target="_blank" rel="noopener">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M2 2h5v5H2zM9 2h5v5H9zM2 9h5v5H2zM9 9h5v5H9z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/>
          </svg>
          Install from VS Code Marketplace
        </a>
        <a id="download-btn"
           class="install-dl-btn-secondary"
           href="https://github.com/tenstorrent/tt-vscode-toolkit/releases/latest">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M8 1v9M4 7l4 4 4-4M2 13h12" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          Download .vsix
        </a>
        <a href="/lessons/" class="install-browse-btn">
          Browse Lessons — no hardware needed →
        </a>
        <span id="dl-version" class="install-version-badge"></span>
      </div>

      <p class="install-cli-note">
        Or via terminal:
        <code>code --install-extension Tenstorrent.tt-vscode-toolkit</code>
      </p>
```

- [ ] **Step 3: Commit**

```bash
git add site/install/index.html
git commit -m "feat: add VS Code Marketplace as primary install CTA on site"
```

---

## Task 3: Add Marketplace install tab to install page

**Files:**
- Modify: `site/install/index.html` (install tabs section, ~lines 914-929)

- [ ] **Step 1: Update the section intro text**

Find (~line 915):
```html
  <p class="install-section-sub">
    The extension ships as a <code>.vsix</code> file and installs the same way in
    VS&nbsp;Code, Cursor, Windsurf, and code-server.
  </p>
```

Replace with:
```html
  <p class="install-section-sub">
    The easiest way is the VS&nbsp;Code Marketplace — one click or one command. A
    <code>.vsix</code> is also available for Cursor, Windsurf, and code-server.
  </p>
```

- [ ] **Step 2: Add Marketplace tab button as first, deactivate VS Code**

Find (~line 920):
```html
  <div class="install-tabs" role="tablist" aria-label="Editor install instructions">
    <button class="install-tab active" role="tab" id="tab-btn-vscode"       aria-selected="true"  aria-controls="tab-vscode"       data-tab="vscode">VS Code</button>
    <button class="install-tab"        role="tab" id="tab-btn-cursor"        aria-selected="false" aria-controls="tab-cursor"        data-tab="cursor">Cursor / Windsurf</button>
    <button class="install-tab"        role="tab" id="tab-btn-code-server"   aria-selected="false" aria-controls="tab-code-server"   data-tab="code-server">code-server</button>
    <button class="install-tab"        role="tab" id="tab-btn-cli"           aria-selected="false" aria-controls="tab-cli"           data-tab="cli">CLI one-liner</button>
  </div>
```

Replace with:
```html
  <div class="install-tabs" role="tablist" aria-label="Editor install instructions">
    <button class="install-tab active" role="tab" id="tab-btn-marketplace"   aria-selected="true"  aria-controls="tab-marketplace"   data-tab="marketplace">Marketplace</button>
    <button class="install-tab"        role="tab" id="tab-btn-vscode"        aria-selected="false" aria-controls="tab-vscode"         data-tab="vscode">VS Code .vsix</button>
    <button class="install-tab"        role="tab" id="tab-btn-cursor"        aria-selected="false" aria-controls="tab-cursor"         data-tab="cursor">Cursor / Windsurf</button>
    <button class="install-tab"        role="tab" id="tab-btn-code-server"   aria-selected="false" aria-controls="tab-code-server"    data-tab="code-server">code-server</button>
    <button class="install-tab"        role="tab" id="tab-btn-cli"           aria-selected="false" aria-controls="tab-cli"            data-tab="cli">CLI one-liner</button>
  </div>
```

- [ ] **Step 3: Add Marketplace tab panel as first, deactivate VS Code panel**

Immediately after the closing `</div>` of the `install-tabs` div, find the VS Code panel opening:
```html
  <!-- VS Code -->
  <div class="install-tab-panel active" id="tab-vscode" role="tabpanel" aria-labelledby="tab-btn-vscode" tabindex="0">
```

Change `active` to remove it from the VS Code panel:
```html
  <!-- VS Code .vsix -->
  <div class="install-tab-panel" id="tab-vscode" role="tabpanel" aria-labelledby="tab-btn-vscode" tabindex="0">
```

And insert the new Marketplace panel **before** the VS Code panel:
```html
  <!-- Marketplace -->
  <div class="install-tab-panel active" id="tab-marketplace" role="tabpanel" aria-labelledby="tab-btn-marketplace" tabindex="0">
    <div class="install-step">
      <span class="install-step-num">1</span>
      <div class="install-step-body">
        <p>Install directly from the VS&nbsp;Code Marketplace with one command:</p>
        <pre class="install-cmd" id="cmd-mkt-install">code --install-extension Tenstorrent.tt-vscode-toolkit</pre>
        <button class="install-cmd-copy" data-target="cmd-mkt-install">Copy</button>
      </div>
    </div>
    <div class="install-step">
      <span class="install-step-num">2</span>
      <div class="install-step-body">
        <p>
          Or open VS&nbsp;Code, press <code>Ctrl+Shift+X</code>, search for
          <strong>TT Developer Toolkit</strong>, and click <strong>Install</strong>.
        </p>
        <p>
          <a href="https://marketplace.visualstudio.com/items?itemName=Tenstorrent.tt-vscode-toolkit"
             target="_blank" rel="noopener">Open in Marketplace →</a>
        </p>
      </div>
    </div>
    <div class="install-step">
      <span class="install-step-num">3</span>
      <div class="install-step-body">
        <p>
          Reload VS&nbsp;Code — the <strong>TT Toolkit</strong> icon will appear in the Activity Bar.
          Click it to open the Lessons panel and pick your hardware.
        </p>
      </div>
    </div>
  </div>

```

- [ ] **Step 4: Commit**

```bash
git add site/install/index.html
git commit -m "feat: add Marketplace tab as default install option on site"
```

---

## Task 4: Update README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add marketplace badge**

Find lines 5-6 (the badge block):
```markdown
[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![VSCode](https://img.shields.io/badge/VSCode-1.93%2B-blue.svg)](https://code.visualstudio.com/)
```

Replace with:
```markdown
[![VS Code Marketplace](https://img.shields.io/visual-studio-marketplace/v/Tenstorrent.tt-vscode-toolkit.svg?label=VS%20Code%20Marketplace)](https://marketplace.visualstudio.com/items?itemName=Tenstorrent.tt-vscode-toolkit)
[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![VSCode](https://img.shields.io/badge/VSCode-1.93%2B-blue.svg)](https://code.visualstudio.com/)
```

- [ ] **Step 2: Restructure the installation options**

Find (starting at line ~98):
```markdown
### Installation

#### Option 1: Install from VSIX Package (Recommended)

```bash
# Download the latest release
# Install extension
code --install-extension tt-vscode-toolkit-*.vsix

# Restart VSCode
```

#### Option 2: Build from Source

```bash
# Clone repository
git clone https://github.com/tenstorrent/tt-vscode-toolkit.git
cd tt-vscode-toolkit

# Install dependencies
npm install

# Build extension
npm run build

# Package extension
npm run package

# Install
code --install-extension tt-vscode-toolkit-*.vsix
```
```

Replace with:
```markdown
### Installation

#### Option 1: VS Code Marketplace (Recommended)

```bash
code --install-extension Tenstorrent.tt-vscode-toolkit
```

Or search **"TT Developer Toolkit"** in the VSCode Extensions panel (`Ctrl+Shift+X`).

→ [Open in VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=Tenstorrent.tt-vscode-toolkit)

#### Option 2: Install from VSIX Package

```bash
# Download the latest release from GitHub
gh release download --repo tenstorrent/tt-vscode-toolkit --pattern '*.vsix'

# Install extension
code --install-extension tt-vscode-toolkit-*.vsix
```

#### Option 3: Build from Source

```bash
# Clone repository
git clone https://github.com/tenstorrent/tt-vscode-toolkit.git
cd tt-vscode-toolkit

# Install dependencies
npm install

# Build and package extension
npm run build
npm run package

# Install
code --install-extension tt-vscode-toolkit-*.vsix
```
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add marketplace as primary install option in README"
```

---

## Task 5: Update release workflow

**Files:**
- Modify: `.github/workflows/release.yml`

- [ ] **Step 1: Update GitHub Release body text**

Find the `### Installation` section inside the release body (starts ~line 58):
```yaml
            ### Installation

            **Manual Installation:**
            1. Download `tt-vscode-toolkit-${{ steps.package_version.outputs.version }}.vsix` below
            2. In VSCode: Extensions → ⋯ → Install from VSIX
            3. Open the welcome page: `Ctrl+Shift+P` → "Tenstorrent: Show Welcome Page"

            **From Source:**
```

Replace with:
```yaml
            ### Installation

            **VS Code Marketplace (Recommended):**
            ```
            code --install-extension Tenstorrent.tt-vscode-toolkit
            ```
            Or search **TT Developer Toolkit** in the Extensions panel.

            **Manual VSIX install:**
            1. Download `tt-vscode-toolkit-${{ steps.package_version.outputs.version }}.vsix` below
            2. In VSCode: Extensions → ⋯ → Install from VSIX
            3. Open the welcome page: `Ctrl+Shift+P` → "Tenstorrent: Show Welcome Page"

            **From Source:**
```

- [ ] **Step 2: Enable the publish-marketplace job**

Find (~line 99):
```yaml
    if: false  # Disabled - enable when ready to publish to marketplace
```

Delete that entire line (the `if:` condition). The job should now read:
```yaml
  publish-marketplace:
    name: Publish to VS Code Marketplace
    runs-on: ubuntu-latest
    needs: create-release
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: enable marketplace auto-publish on tagged releases"
```

---

## Task 6: Bump version and update CHANGELOG

**Files:**
- Modify: `package.json`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Bump version in package.json**

In `package.json`, change:
```json
  "version": "0.0.406",
```
to:
```json
  "version": "0.0.407",
```

- [ ] **Step 2: Add CHANGELOG entry**

In `CHANGELOG.md`, insert after the `---` separator on line 9 (before the `## [0.0.406]` entry):

```markdown
## [0.0.407] - 2026-04-24

### Added

- **Marketplace publishing** — extension now auto-publishes to the VS Code Marketplace on every tagged release via the `publish-marketplace` CI job in `release.yml`
- **VS Code Marketplace badge** in `README.md` linking to the marketplace listing
- **Marketplace install option** added as Option 1 (Recommended) in `README.md` installation section
- **Marketplace hero button** on the site install page (`site/install/index.html`) as the primary CTA
- **Marketplace install tab** added as the default tab in the site install instructions

### Fixed

- **`scripts/build-web.js`** — corrected wrong extension ID in `mktUrl` from `Tenstorrent.tenstorrent-toolkit` to `Tenstorrent.tt-vscode-toolkit`; regenerated all site lesson pages

---
```

- [ ] **Step 3: Rebuild extension**

```bash
npm run build
```

Expected: exits 0, no errors.

- [ ] **Step 4: Commit**

```bash
git add package.json CHANGELOG.md
git commit -m "chore: bump version to 0.0.407"
```
